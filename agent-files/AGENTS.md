# Agent Instructions — Todo

Todo is an evolving task-list application. The current implementation is a small HTML/CSS/JavaScript application.

## Before Working

1. Read `agent-files/Agents_Context.md` for current working state.
2. Read `agent-files/ARCHITECTURE.md` for finalized decisions.
3. Inspect the existing source before changing it.
4. Do not assume future redesigns or framework migrations are implemented unless the source confirms them.

## Documentation Authority

- `ARCHITECTURE.md` is authoritative for finalized architecture.
- `Agents_Context.md` is temporary working context.
- Do not silently promote plans or hypotheses into architecture.

## Implementation Rules

Preserve existing behavior unless the requested change requires otherwise. Keep structural/documentation work separate from unrelated application changes.
